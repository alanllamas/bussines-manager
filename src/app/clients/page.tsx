// /clients — Server shell. Muestra estado vacío hasta que el usuario selecciona un cliente en el sidebar.
import ClientsPage from "./page-client";

export default async function Clients() {
  return <ClientsPage />
}